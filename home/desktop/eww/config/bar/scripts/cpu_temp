#!/bin/sh

# CPU temperature (using the first thermal zone found)
temp=$(cat /sys/class/thermal/thermal_zone*/temp 2>/dev/null | head -n 1)
if [ -z "$temp" ]; then
    echo "N/A"
else
    # Convert millidegrees to degrees Celsius
    echo "$(echo "scale=1; $temp / 1000" | bc)"
fi
