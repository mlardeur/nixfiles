# Agent Instructions

This repository contains a Nix configuration to enable `eww`.

## Build and Run
- The `default.nix` enables the `eww` package via `programs.eww`.
- Configuration for `eww` should be placed in the directory specified by `configDir` (currently commented out in `default.nix`).
